<link href="~/Content/bootstrap.css" rel="stylesheet" />
@model MvcMyMenu.Models.Categories[]
@{
    ViewBag.Title = "Details";
}

<table class="table table-hover">
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(category.Name, "Restaurants", new { category = category.Id })
            </tr>
        }
       
    </tbody>
</table> 
<img src="~/Image/392355-blackangel.jpg" class="img-thumbnail" height="500px" width="500px" />
